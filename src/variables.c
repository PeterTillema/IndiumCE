#include "variables.h"

#include <stdint.h>

float variables[26] = {0};

uint8_t* strings[10] = {0};

float* lists[6] = {0};

float* matrices[10] = {0};

struct custom_list custom_lists[50];

void get_all_os_variables(void) {
    // todo
}
